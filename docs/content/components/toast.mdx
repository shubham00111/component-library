---
title: Toast
---

import { loadSnippets } from "../../lib/snippets";
import { SnippetViewer } from "../../components/SnippetViewer";
import { ToastPreview } from "../../components/ToastPreview";
import { NeobrutalismToast, ShadcnToast, FlowbiteToast } from "@lib/components/Toast";

export const snippets = loadSnippets("Toast");

# Toast

Fixed-position notification overlay. Variants: `success`, `error`, `warning`, `info`. Auto-dismiss via `timer` (ms).

> Toasts use `position: fixed` and appear at the corner of the viewport in production. The previews below are contained to the card for documentation purposes.

## Neobrutalism

<SnippetViewer react={snippets.neobrutalism.react} preview={<ToastPreview><NeobrutalismToast iconType="success" position="bottom-right">Changes saved successfully.</NeobrutalismToast></ToastPreview>} />

## Shadcn-inspired

<SnippetViewer react={snippets.shadcn.react} preview={<ToastPreview><ShadcnToast iconType="info" position="bottom-right">Your session will expire in 5 minutes.</ShadcnToast></ToastPreview>} />

## Flowbite-inspired

<SnippetViewer react={snippets.flowbite.react} preview={<ToastPreview><FlowbiteToast iconType="warning" position="bottom-right">Connection unstable.</FlowbiteToast></ToastPreview>} />
