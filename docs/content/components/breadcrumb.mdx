---
title: Breadcrumb
---

import { loadSnippets } from "../../lib/snippets";
import { ComponentShowcase } from "../../components/ComponentShowcase";
import { SnippetViewer } from "../../components/SnippetViewer";
import {
  NeobrutalismBreadcrumbPreview,
  ShadcnBreadcrumbPreview,
  FlowbiteBreadcrumbPreview,
  GlassmorphismBreadcrumbPreview,
  Md3BreadcrumbPreview,
  NmBreadcrumbPreview,
} from "../../components/BreadcrumbVariants";

export const snippets = loadSnippets("Breadcrumb");

# Breadcrumb

Navigation hierarchy trail. Compound sub-components: `Breadcrumb` + `Breadcrumb.Item`. The last item (no `href`) is automatically marked as current page with `aria-current="page"`.

## Design Language Comparison

<ComponentShowcase
  variants={[
    {
      label: "Neobrutalism",
      designLanguage: "neobrutalism",
      children: <NeobrutalismBreadcrumbPreview />,
    },
    {
      label: "Shadcn-inspired",
      designLanguage: "shadcn",
      children: <ShadcnBreadcrumbPreview />,
    },
    {
      label: "Flowbite-inspired",
      designLanguage: "flowbite",
      children: <FlowbiteBreadcrumbPreview />,
    },
    {
      label: "Glassmorphism",
      designLanguage: "glassmorphism",
      children: <GlassmorphismBreadcrumbPreview />,
    },
    {
      label: "Material Design 3",
      designLanguage: "md3",
      children: <Md3BreadcrumbPreview />,
    },
    {
      label: "Neumorphism",
      designLanguage: "neumorphism",
      children: <NmBreadcrumbPreview />,
    },
  ]}
  layout="column"
/>

## Neobrutalism

Bold text, `/` separator, yellow underline hover on links.

<SnippetViewer react={snippets.neobrutalism.react} preview={<NeobrutalismBreadcrumbPreview />} />

## Shadcn-inspired

Zinc muted link text, `/` separator, zinc-900 current item.

<SnippetViewer react={snippets.shadcn.react} preview={<ShadcnBreadcrumbPreview />} />

## Flowbite-inspired

Blue links (`#1c64f2`), `›` separator, gray current item.

<SnippetViewer react={snippets.flowbite.react} preview={<FlowbiteBreadcrumbPreview />} />

## Glassmorphism

Translucent white links, `/` separator, white current item on gradient background.

<SnippetViewer react={snippets.glassmorphism.react} preview={<GlassmorphismBreadcrumbPreview />} />

## Material Design 3

Purple tinted links with hover tonal pill, `›` separator, on-surface current item.

<SnippetViewer react={snippets.md3.react} preview={<Md3BreadcrumbPreview />} />

## Neumorphism

Blue-gray semibold links, faded `/` separator, darker current item.

<SnippetViewer react={snippets.neumorphism.react} preview={<NmBreadcrumbPreview />} />
